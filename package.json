{
  "name": "tying.ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve -s . -l 3000",
    "build": "bash build.sh",
    "build:simple": "rm -rf dist && mkdir -p dist && cp -r report position wiki components scripts assets dist/ && cp ./*.html ./*.xml ./*.txt ./*.json ./*.ico ./*.css ./*.js dist/ 2>/dev/null || true",
    "start": "serve -s dist -l 3000",
    "clean": "rm -rf dist .next"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "html-minifier": "^4.0.0",
    "clean-css-cli": "^5.6.3",
    "terser": "^5.36.0"
  }
}
 
